-- ============================================================
-- МИГРАЦИЯ 3: всички параметри стават числови
-- Пусни това в Supabase -> SQL Editor -> New query -> Run
-- (безопасно е, не трие съществуващи клиенти или история)
-- ============================================================

update parameters set value_type = 'number' where value_type <> 'number';
